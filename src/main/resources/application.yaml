#######################
# Server Configuration
#######################
server:
  port: ${SIAMOIS_PORT:8099}
  forward-headers-strategy: native
  # application/xml couvre les réponses partielles de Faces : sans ce type, l'essentiel du gain est perdu.
  compression:
    enabled: true
    mime-types: text/html,text/plain,text/css,text/javascript,application/javascript,application/xml,text/xml,application/json,image/svg+xml,font/ttf
    min-response-size: 1024
  servlet:
    context-path: ${CONTEXT_PATH:/siamois}
    session:
      timeout: 125m
  error:
    path: /error/500
    whitelabel:
      enabled: false

siamois:
  lang:
    default: ${DEFAULT_LANG:fr}
    available: en, fr
  admin:
    username: ${SIAMOIS_ADMIN_LOGIN}
    password: ${SIAMOIS_ADMIN_PASSWORD}
    email: ${SIAMOIS_ADMIN_EMAIL}
  documents:
    folder-path: ${SIAMOIS_DOCUMENTS_PATH}
    allowed-types: image/jpeg, image/png, image/svg+xml, image/tiff, image/webp, image/apng, application/pdf
  jwt:
    # HS256 : au moins 256 bits — utiliser une valeur forte en prod (variable SIAMOIS_JWT_SECRET).
    secret: ${SIAMOIS_JWT_SECRET:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
    access-token-validity: PT15M


spring:
  thymeleaf:
    check-template-location: false
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      # Au-delà du max_connections de PostgreSQL (100 par défaut), les connexions supplémentaires
      # ne peuvent pas s'ouvrir : un pool dimensionné large ne fait que déplacer l'attente.
      maximum-pool-size: 25
      minimum-idle: 5
      idle-timeout: 30000
      connection-test-query: SELECT 1
      data-source-properties:
        reWriteBatchedInserts: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
        # Regroupe le chargement des associations paresseuses et évite le N+1 sur les listes.
        default_batch_fetch_size: 32
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
        envers:
          audit_table_suffix: _AUD
          store_data_at_delete: true
          global_with_modified_flag: true

  liquibase:
    enabled: true
    change-log: classpath:/changelog/db.changelog.xml

  messages:
    basename: language/messages
    encoding: UTF-8
    fallback-to-system-locale: false

  web:
    resources:
      static-locations: classpath:/static/

  servlet:
    multipart:
      enabled: true
      max-file-size: 8MB
      max-request-size: 8MB
  cache:
    caffeine:
      spec: expireAfterAccess=1h,maximumSize=500

  mail:
    properties:
      mail:
        smtp:
          auth: ${SMTP_AUTH_ENABLED:true}
        sender: ${EMAIL_SENDER_ADDRESS}
    host: ${SMTP_HOST}
    port: ${SMTP_PORT}
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}

joinfaces:
  faces:
    project-stage: production
  primefaces:
    theme: overcast

logging:
  level:
    root: INFO
    org:
      springframework: INFO
      hibernate:
        SQL: INFO

springdoc:
  swagger-ui:
    tags-sorter: alpha
    operations-sorter: alpha
    doc-expansion: none
    display-request-duration: true
  default-produces-media-type: application/json

